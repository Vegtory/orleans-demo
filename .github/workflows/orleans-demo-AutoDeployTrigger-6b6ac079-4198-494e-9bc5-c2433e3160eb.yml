name: Trigger auto deployment for orleans-demo

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/orleans-demo-AutoDeployTrigger-6b6ac079-4198-494e-9bc5-c2433e3160eb.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.ORLEANSDEMO_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.ORLEANSDEMO_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.ORLEANSDEMO_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          registryUrl: orleansdemo.azurecr.io
          registryUsername: ${{ secrets.ORLEANSDEMO_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.ORLEANSDEMO_REGISTRY_PASSWORD }}
          containerAppName: orleans-demo
          resourceGroup: orleans-demo-sweden
          imageToBuild: orleansdemo.azurecr.io/orleans-demo:${{ github.sha }}


